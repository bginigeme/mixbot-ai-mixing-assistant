# Requirements for the MixBot MCP server (mcp_server.py)
# Requires Python 3.10+ — use the mcp_env virtualenv in this project.
#
# Setup:
#   source $HOME/.local/bin/env   # activate uv (if needed)
#   uv venv mcp_env --python 3.13
#   uv pip install -r mcp_requirements.txt --python mcp_env/bin/python

mcp[cli]>=1.0.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0
